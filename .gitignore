###############################
# StampVault .gitignore
###############################

# OS / Editor
.DS_Store
Thumbs.db
.vscode/
.idea/
*.sublime-workspace
*.sublime-project
*.swp
*.swo

# Node / JS
node_modules/
npm-debug.log*
*.log

# PHP / Composer
composer.phar
vendor/
# Keep composer.lock for reproducible installs (do NOT ignore)

# Environment / Secrets
.env
.env.*

# Archives / Temp
*.zip
*.tar.gz
*.rar
*.bak
*.tmp

# WordPress core (not part of plugin repo)
/wp-admin/
/wp-includes/

# Caches / Uploads (only relevant if repo root is WP root; kept for safety)
wp-content/uploads/
wp-content/cache/

# Build output: NOT committed (option 3 strategy). Run `npm run build` before packaging a release zip.
dist/
build/

# Plugin-specific local config
stampvault-settings.json
# Allow lock files for reproducible builds
# (Remove below lines so composer.lock & package-lock.json are tracked)
composer.lock
package-lock.json
*.lock
